name: Local suites

# Runs the repository's offline checks on every pull request and on main.
# Nothing here talks to Salesforce, GitHub Pages or any live service.
on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Generated metadata matches the generator
        run: |
          node scripts/build-metadata.js
          git diff --exit-code --stat
          test -z "$(git status --porcelain)"
      - name: Test suites
        run: npm test
